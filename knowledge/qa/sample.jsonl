{"instruction":"Review this TypeScript code for security issues: Hardcoded JWT secret key found in source code. This poses a security risk as anyone with access to the code can forge JWT tokens.","output":"HIGH HARDCODED_SECRET: Hardcoded JWT secret key in src/auth/jwt.ts line 42 presents a critical security vulnerability. Anyone with access to the source code can forge valid JWT tokens, potentially leading to privilege escalation, unauthorized access to protected resources, and complete authentication bypass.\n\nImpact:\n- Complete authentication system compromise\n- Ability to impersonate any user\n- Unauthorized access to admin/functionality\n- Potential data breach and privilege escalation\n\nFix:\n1. Move secret to environment variable:\n```typescript\n// BEFORE:\nimport jwt from 'jsonwebtoken';\nconst secret = 'supersecretkey123'; // Hardcoded in source\n\n// AFTER:\nimport jwt from 'jsonwebtoken';\nconst secret = process.env.JWT_SECRET;\n\nif (!secret) {\n  throw new Error('JWT_SECRET environment variable is required');\n}\n```\n\n2. Use secure random generation for development defaults (never commit to source):\n```typescript\n// Only for development - never use in production!\nconst secret = process.env.JWT_SECRET || crypto.randomBytes(32).toString('hex');\n\n// Better: require explicit configuration\nconst secret = process.env.JWT_SECRET;\nif (!secret && process.env.NODE_ENV === 'production') {\n  throw new Error('JWT_SECRET must be set in production');\n}\n```\n\n3. Implement key rotation strategy:\n```typescript\n// Support multiple secrets for smooth rotation\nconst secrets = [\n  process.env.JWT_CURRENT_SECRET,\n  process.env.JWT_PREVIOUS_SECRET\n].filter(Boolean);\n\n// Try each secret when verifying\nfunction verifyToken(token: string) {\n  for (const secret of secrets) {\n    try {\n      return jwt.verify(token, secret);\n    } catch (_) {\n      // Continue trying other secrets\n    }\n  }\n  throw new Error('Invalid token');\n}\n```\n\n4. Add validation to ensure secret meets minimum strength requirements:\n```typescript\nfunction validateSecret(secret: string): boolean {\n  return secret.length >= 32; // Minimum 256-bit key\n}\n\nif (!validateSecret(secret)) {\n  throw new Error('JWT_SECRET must be at least 32 characters long');\n}\n```\n\n5. Use secrets management systems in production:\n- AWS Secrets Manager\n- HashiCorp Vault\n- Kubernetes Secrets\n- Azure Key Vault\n\n6. Audit and rotate all instances where the hardcoded secret may have been exposed\n\nReferences:\n- OWASP Authentication Cheat Sheet\n- RFC 7519: JSON Web Token (JWT)\n- NIST SP 800-63B: Digital Identity Guidelines","source":"sample","category":"security"}
