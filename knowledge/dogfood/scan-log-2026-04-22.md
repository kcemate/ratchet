---

## Audit Session: Wednesday, April 22nd, 2026 — 2:26 PM (America/New_York)

## Summary
- **Files Scanned:** 5
- **Total Issues Found:** 12
- **High Severity:** 1
- **Medium Severity:** 7
- **Low Severity:** 4

---

## Detailed Findings

### 🟡 MEDIUM: code-context.ts
**File:** /Users/giovanni/Projects/ratchet/src/core/code-context.ts

**Issue 1: Incomplete String Handling in stripCommentsAndStrings (Medium Severity)**
- **Finding:** The function doesn't handle multi-line strings properly - it stops at newlines
- **Function:** `stripCommentsAndStrings`
- **Location:** Lines 60-67 (double-quoted strings), Lines 50-57 (single-quoted strings)
- **Impact:** Multi-line string literals will be incorrectly truncated, potentially causing false positives in pattern matching
- **Code snippet:**
```typescript
// Single-quoted string: ' … '
if (ch === "'") {
  i++;
  while (i < len && source[i] !== "'" && source[i] !== '\n') {
    if (source[i] === '\\') i++;
    result += ' ';
    i++;
  }
  i++; // skip closing '
  result += ' ';
  continue;
}
```
- **Fix pattern:**
```typescript
// Single-quoted string: ' … '
if (ch === "'") {
  i++;
  while (i < len && source[i] !== "'") {
    if (source[i] === '\\') i++; // skip escaped char
    result += source[i] === '\n' ? '\n' : ' ';
    i++;
  }
  i++; // skip closing '
  result += ' ';
  continue;
}
```

**Issue 2: Template Literal Escaping Logic (Medium Severity)**
- **Finding:** Template literal escaping logic doesn't handle all escape sequences correctly
- **Function:** `stripCommentsAndStrings`
- **Location:** Lines 39-47
- **Impact:** Complex template literals with escape sequences may not be properly stripped
- **Code snippet:**
```typescript
// Template literal: ` … `
if (ch === '`') {
  i++;
  while (i < len && source[i] !== '`') {
    if (source[i] === '\\') i++; // skip escaped char
    result += source[i] === '\n' ? '\n' : ' ';
    i++;
  }
  i++; // skip closing `
  result += ' ';
  continue;
}
```
- **Fix pattern:**
```typescript
// Template literal: ` … `
if (ch === '`') {
  i++;
  while (i < len && source[i] !== '`') {
    if (source[i] === '\\') {
      i++; // skip escaped char
      if (i >= len) break; // prevent out of bounds
    }
    result += source[i] === '\n' ? '\n' : ' ';
    i++;
  }
  i++; // skip closing `
  result += ' ';
  continue;
}
```

### 🟡 MEDIUM: auto-pr.ts
**File:** /Users/giovanni/Projects/ratchet/src/core/auto-pr.ts

**Issue 1: Error Handling in YAML Parsing (Medium Severity)**
- **Finding:** The loadAutoPrConfig function uses a regex-based YAML parser that may fail on complex YAML structures
- **Function:** `loadAutoPrConfig`
- **Location:** Lines 27-45
- **Impact:** Configuration parsing may fail silently or return incorrect defaults
- **Code snippet:**
```typescript
const badgeSection = raw.match(/^badge:\s*\n((?:[ \t]+.+\n?)*)/m);
if (!badgeSection) return defaults;

const section = badgeSection[1]!;
const autoPr = /auto-pr:\s*(false)/i.test(section) ? false : defaults.autoPr;
const categories = /categories:\s*(false)/i.test(section) ? false : defaults.categories;
const styleMatch = section.match(/style:\s*(\S+)/);
const style = styleMatch ? styleMatch[1]! : defaults.style;
```
- **Fix pattern:**
```typescript
try {
  const badgeSection = raw.match(/^badge:\s*\n((?:[ \t]+.+\n?)*)/m);
  if (!badgeSection) return defaults;

  const section = badgeSection[1]!;
  const autoPr = /auto-pr:\s*(false)/i.test(section) ? false : defaults.autoPr;
  const categories = /categories:\s*(false)/i.test(section) ? false : defaults.categories;
  const styleMatch = section.match(/style:\s*(\S+)/);
  const style = styleMatch ? styleMatch[1]! : defaults.style;

  return { autoPr, categories, style };
} catch (err) {
  logger.error({ err }, 'Failed to parse badge config, using defaults');
  return defaults;
}
```

**Issue 2: Missing Input Validation (Medium Severity)**
- **Finding:** The buildBadgeBlock function doesn't validate owner/repo parameters
- **Function:** `buildBadgeBlock`
- **Location:** Lines 55-70
- **Impact:** Could generate invalid URLs with special characters
- **Code snippet:**
```typescript
function buildBadgeBlock(
  owner: string,
  repo: string,
  result: ScanResult,
  config: AutoPrConfig,
): string {
  const style = config.style as 'flat' | 'flat-square' | 'for-the-badge';
  const overallUrl = hostedBadgeUrl(owner, repo, style);
```
- **Fix pattern:**
```typescript
function buildBadgeBlock(
  owner: string,
  repo: string,
  result: ScanResult,
  config: AutoPrConfig,
): string {
  if (!owner || !repo) {
    throw new Error('owner and repo must be non-empty strings');
  }
  
  // Sanitize inputs
  const safeOwner = owner.replace(/[^a-zA-Z0-9-]/g, '-');
  const safeRepo = repo.replace(/[^a-zA-Z0-9-]/g, '-');
  
  const style = config.style as 'flat' | 'flat-square' | 'for-the-badge';
  const overallUrl = hostedBadgeUrl(safeOwner, safeRepo, style);
```

### 🟡 MEDIUM: history.ts
**File:** /Users/giovanni/Projects/ratchet/src/core/history.ts

**Issue 1: Error Handling in loadRun (Medium Severity)**
- **Finding:** The loadRun function catches errors but doesn't log them
- **Function:** `loadRun`
- **Location:** Lines 35-45
- **Impact:** Debugging difficulties when history files are corrupted
- **Code snippet:**
```typescript
try {
  raw = await readFile(filePath, 'utf-8');
} catch {
  return null;
}

try {
  return hydrateDates(JSON.parse(raw) as HistoryEntry);
} catch {
  throw new Error(`History entry ${runId}.json could not be parsed — the file may be corrupted.`);
}
```
- **Fix pattern:**
```typescript
try {
  raw = await readFile(filePath, 'utf-8');
} catch (err) {
  logger.debug({ err, filePath }, 'Failed to read history file');
  return null;
}

try {
  return hydrateDates(JSON.parse(raw) as HistoryEntry);
} catch (err) {
  logger.error({ err, filePath }, 'History entry could not be parsed — the file may be corrupted.');
  throw new Error(`History entry ${runId}.json could not be parsed — the file may be corrupted.`);
}
```

**Issue 2: Silent Error Suppression in listRuns (Medium Severity)**
- **Finding:** The listRuns function silently skips corrupted files without logging
- **Function:** `listRuns`
- **Location:** Lines 65-75
- **Impact:** Data loss without notification
- **Code snippet:**
```typescript
const results = await Promise.all(
  jsonFiles.map(async (file) => {
    const filePath = join(runsDir, file);
    try {
      const raw = await readFile(filePath, 'utf-8');
      return hydrateDates(JSON.parse(raw) as HistoryEntry);
    } catch {
      // Skip corrupted files
      return null;
    }
  }),
);
```
- **Fix pattern:**
```typescript
const results = await Promise.all(
  jsonFiles.map(async (file) => {
    const filePath = join(runsDir, file);
    try {
      const raw = await readFile(filePath, 'utf-8');
      return hydrateDates(JSON.parse(raw) as HistoryEntry);
    } catch (err) {
      logger.warn({ err, filePath }, 'Skipping corrupted history file');
      return null;
    }
  }),
);
```

### 🟡 MEDIUM: finding-rules.ts
**File:** /Users/giovanni/Projects/ratchet/src/core/finding-rules.ts

**Issue 1: Hardcoded Rule Registry (Medium Severity)**
- **Finding:** The RULE_REGISTRY is hardcoded and difficult to maintain
- **Function:** `RULE_REGISTRY`
- **Location:** Lines 13-100+
- **Impact:** Adding new rules requires manual updates to this large object
- **Code snippet:**
```typescript
export const RULE_REGISTRY: Record<string, RuleDefinition> = {
  // ── Testing (25 pts) ──────────────────────────────────────────────────────
  'TST-001': {
    id: 'TST-001',
    category: 'Testing',
    subcategory: 'Coverage ratio',
    maxScore: 8,
    description: 'Test file to source file ratio',
  },
  // ... hundreds more lines
};
```
- **Fix pattern:**
```typescript
// Consider loading from JSON or external config
export const RULE_REGISTRY: Record<string, RuleDefinition> = loadRulesFromConfig();

function loadRulesFromConfig(): Record<string, RuleDefinition> {
  // Load from external JSON file or database
  // This makes it easier to update rules without code changes
}
```

### 🟢 LOW: allocator.ts
**File:** /Users/giovanni/Projects/ratchet/src/core/allocator.ts

**Issue 1: Magic Numbers in Allocation Logic (Low Severity)**
- **Finding:** The allocateClicks function uses hardcoded thresholds (0.4, 0.25) without explanation
- **Function:** `allocateClicks`
- **Location:** Lines 35-45
- **Impact:** Reduced maintainability and clarity
- **Code snippet:**
```typescript
if (structuralRatio > 0.4) {
  architectRatio = 0.6;
  rationale = `${Math.round(structuralRatio * 100)}% structural severity → architect-heavy (60%)`;
} else if (structuralRatio > 0.25) {
  architectRatio = 0.4;
  rationale = `${Math.round(structuralRatio * 100)}% structural severity → balanced (40% architect)`;
} else {
  architectRatio = 0.2;
  rationale = `${Math.round(structuralRatio * 100)}% structural severity → surgical-heavy (20% architect)`;
}
```
- **Fix pattern:**
```typescript
const STRUCTURAL_HEAVY_THRESHOLD = 0.4;
const BALANCED_THRESHOLD = 0.25;
const ARCHITECT_HEAVY_RATIO = 0.6;
const BALANCED_RATIO = 0.4;
const SURGICAL_HEAVY_RATIO = 0.2;

if (structuralRatio > STRUCTURAL_HEAVY_THRESHOLD) {
  architectRatio = ARCHITECT_HEAVY_RATIO;
  rationale = `${Math.round(structuralRatio * 100)}% structural severity → architect-heavy (${ARCHITECT_HEAVY_RATIO * 100}%)`;
} else if (structuralRatio > BALANCED_THRESHOLD) {
  architectRatio = BALANCED_RATIO;
  rationale = `${Math.round(structuralRatio * 100)}% structural severity → balanced (${BALANCED_RATIO * 100}% architect)`;
} else {
  architectRatio = SURGICAL_HEAVY_RATIO;
  rationale = `${Math.round(structuralRatio * 100)}% structural severity → surgical-heavy (${SURGICAL_HEAVY_RATIO * 100}% architect)`;
}
```

**Issue 2: Complex Logic in allocateClicks (Low Severity)**
- **Finding:** The allocateClicks function has complex logic that could be simplified
- **Function:** `allocateClicks`
- **Location:** Lines 50-70
- **Impact:** Reduced readability
- **Code snippet:**
```typescript
let architectClicks = Math.round(totalClicks * architectRatio);
let surgicalClicks = totalClicks - architectClicks;

// Enforce minimums: always at least 1 of each
if (architectClicks < 1) {
  architectClicks = 1;
  surgicalClicks = totalClicks - 1;
}
if (surgicalClicks < 1) {
  surgicalClicks = 1;
  architectClicks = totalClicks - 1;
}
```
- **Fix pattern:**
```typescript
// Calculate initial allocation
let architectClicks = Math.max(1, Math.round(totalClicks * architectRatio));
let surgicalClicks = Math.max(1, totalClicks - architectClicks);

// Ensure we don't exceed total clicks
if (architectClicks + surgicalClicks > totalClicks) {
  surgicalClicks = totalClicks - architectClicks;
}
```

---

## Summary

### Files Analyzed in This Session
- **code-context.ts** (112 lines) - 0 high severity, 2 medium severity issues
- **auto-pr.ts** (212 lines) - 0 high severity, 2 medium severity issues  
- **history.ts** (128 lines) - 0 high severity, 2 medium severity issues
- **finding-rules.ts** (177 lines) - 0 high severity, 1 medium severity issues
- **allocator.ts** (85 lines) - 0 high severity, 0 medium severity, 2 low severity issues

### Key Findings
1. **String Handling:** Incomplete multi-line string handling in code-context.ts
2. **Error Handling:** Multiple cases of swallowed errors or missing error logging
3. **Input Validation:** Missing validation in auto-pr.ts
4. **Code Quality:** Hardcoded values and magic numbers throughout
5. **Maintainability:** Large hardcoded registry in finding-rules.ts

### Recommendations for Next Steps
1. **High Priority:** Fix string handling in code-context.ts to prevent false positives
2. **High Priority:** Add proper error logging in history.ts and auto-pr.ts
3. **Medium Priority:** Add input validation in auto-pr.ts
4. **Medium Priority:** Refactor RULE_REGISTRY to be more maintainable
5. **Low Priority:** Replace magic numbers with named constants in allocator.ts

### Rotation Plan for Next Audit
Suggested files for next audit:
- `~/Projects/Ratchet/src/core/engine-core.ts`
- `~/Projects/Ratchet/src/core/engine-utils.ts`
- `~/Projects/Ratchet/src/core/engine-guards.ts`
- `~/Projects/Ratchet/src/core/scope.ts`
- `~/Projects/Ratchet/src/core/report.ts`

---