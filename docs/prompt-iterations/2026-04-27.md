# Prompt Iteration: 2026-04-27

## Agent: Shell Agent (buildIssuePlanPrompt)

## Failure Pattern Addressed
SEARCH blocks not matching exactly due to whitespace/character mismatches, leading to "Search text not found" errors and skipped edits in F1 worker runs.

**Evidence from F1 Worker Logs (April 1, 2026):**
- fastify-torque.log: `[APIAgent] Search text not found in lib/hooks.js (127 chars) — skipping edit`
- fastify-torque.log: `[APIAgent] 1 EDIT block(s) failed — search text not found in target file(s). Click skipped.`

This indicates agents are generating SEARCH blocks that don't exactly match what's in the target file, likely due to:
- Inaccurate line copying (missing/extra characters)
- Whitespace mismatches (spaces vs tabs, indentation differences)
- Incorrect line counts or context selection

## Root Cause Analysis
While the previous iteration (April 26th) fixed the issue of extra characters being output after the REPLACE: block (which caused torque exit code 2), a remaining issue is agents not copying SEARCH blocks with sufficient precision. The SEARCH block must match the target file EXACTLY - every space, tab, and line ending must be identical.

Agents may be:
- Copying approximate lines rather than exact matches
- Not verifying whitespace sensitivity
- Lacking specific techniques to confirm exact matching before outputting

## Improvement Applied
Enhanced `buildIssuePlanPrompt` in `/Users/giovanni/Projects/ratchet/src/core/agents/shell.ts` with more precise SEARCH block verification guidance:

### Added to SEARCH BLOCK RULES:
```
- Whitespace matters: every space, tab, and line ending must match EXACTLY
- Use your editor's line/column indicators to verify exact copying
- To verify: paste SEARCH block into a temporary file and use diff to check against original
- If unsure about exact lines, use MORE context (8-10 lines) rather than fewer
```

This builds upon yesterday's improvements which already included:
- Basic verification instruction: "Verify your SEARCH block appears VERBATIM in the current file before outputting"
- Context guidance: "Copy EXACTLY 5-10 consecutive lines from the current file"
- Marker exclusion rules: "Do NOT include the `<<<<<<< SEARCH` or `=======` or `>>>>>>> REPLACE` markers inside either block"

### Enhanced Verification Techniques:
1. **Whitespace sensitivity awareness** - Explicitly call out that whitespace differences cause mismatches
2. **Editor tool usage** - Leverage built-in editor features for precise line/column selection
3. **Practical verification method** - Suggest using diff tool to confirm exact match before outputting
4. **Conservative context recommendation** - Advise using more context lines when uncertain

## Expected Outcome
This change should reduce "Search text not found" errors by:
1. Making agents explicitly aware of whitespace sensitivity requirements
2. Providing concrete verification techniques they can implement
3. Encouraging conservative context selection when uncertain
4. Reducing guesswork in SEARCH block creation

## Files Modified
- `src/core/agents/shell.ts` - Enhanced SEARCH BLOCK RULES with verification guidance
- `docs/prompt-iterations/2026-04-27.md` - This iteration log

## Test Results
Ran full test suite: 3117 tests total
- 20 failed (pre-existing guard resolution issues, not related to prompt changes)
- 3084 passed
- 13 skipped

The failing tests appear to be in `engine-guards.test.ts` and relate to guard resolution logic, not to the prompt formatting changes made in this iteration.

## Next Steps
- Monitor subsequent F1 worker runs for reduction in "Search text not found" occurrences
- Track improvement in successful edit rates across all repos
- If pattern persists, consider adding more specific examples or validation logic