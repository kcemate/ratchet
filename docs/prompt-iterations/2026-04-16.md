# Prompt Iteration - 2026-04-16

## Failure Pattern
From the F1 worker logs (2026-04-01-180247.log), we observed that Torque exited with code 2 for multiple repos (express, fastify, got, hono, trpc), resulting in no delta (score unchanged). The torque logs show rollback messages indicating the LLM output was not the expected `MODIFIED: <filepath>` line but extraneous characters (e.g., "}"). This confirms the failure pattern: extra output from the LLM causing validation failure.

## Original Prompt Snippet (from src/core/agents/shell.ts lines 368-374)
```
    `HARD CONSTRAINTS (violating these will cause rollback):\n` +
    `${constraints}\n` +
    `- Do NOT refactor, restructure, or rewrite functions\n` +
    `- Do NOT rename variables, extract helpers, or "improve" unrelated code\n` +
    `- Do NOT add new dependencies or change public function signatures\n` +
    `- Do NOT change formatting, whitespace, or style in untouched lines\n\n` +
    `MODIFIED: <filepath>`
```

## Improved Prompt Snippet
Added a constraint to forbid any extra output, ensuring the LLM outputs only the required line.

```
    `HARD CONSTRAINTS (violating these will cause rollback):\n` +
    `${constraints}\n` +
    `- Do NOT refactor, restructure, or rewrite functions\n` +
    `- Do NOT rename variables, extract helpers, or "improve" unrelated code\n` +
    `- Do NOT add new dependencies or change public function signatures\n` +
    `- Do NOT change formatting, whitespace, or style in untouched lines\n` +
    `- ANY EXTRA OUTPUT WILL CAUSE ROLLBACK. Output ONLY the line 'MODIFIED: <filepath>' and nothing else.\\n\\n` +
    `MODIFIED: <filepath>`
```

## Reasoning
The torque validation expects the exact output format. By explicitly forbidding extra output and reminding the model to output only the modification line, we reduce the chance of verbose explanations, markdown formatting, or stray characters that would break the validation. The extra newline before the MODIFIED line ensures separation.

## Application Status
As of 2026-04-15, we attempted to apply the change to src/core/agents/shell.ts but the applied change may contain escaping errors. Today we need to verify and correct the source file. The plan is to:
1. Verify the current state of the constraint lines.
2. If malformed, revert to backup and apply the corrected change.
3. Run a quick test by invoking the F1 worker on a single repo (e.g., express) to confirm torque no longer exits with code 2 due to extra output.
4. If successful, commit the change with the appropriate message.

## Next Steps
- Verify source file.
- Apply correct change if needed.
- Run test.
- Commit.