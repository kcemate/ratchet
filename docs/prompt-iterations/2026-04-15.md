# Prompt Iteration - 2026-04-15

## Failure Pattern
From the F1 worker logs (2026-04-01-180247.log), we observed that Torque exited with code 2 for multiple repos (express, fastify, got, hono, trpc), resulting in no delta (score unchanged). The pattern indicates that the LLM's response included extra output beyond the expected `MODIFIED: <filepath>` line, causing the torque validation to fail and rollback the change.

## Original Prompt Snippet (from src/core/agents/shell.ts lines 368-374)
```
    `HARD CONSTRAINTS (violating these will cause rollback):\n` +
    `${constraints}\n` +
    `- Do NOT refactor, restructure, or rewrite functions\n` +
    `- Do NOT rename variables, extract helpers, or "improve" unrelated code\n` +
    `- Do NOT add new dependencies or change public function signatures\n` +
    `- Do NOT change formatting, whitespace, or style in untouched lines\n\n` +
    `MODIFIED: <filepath>`
```

## Improved Prompt Snippet
Added a constraint to forbid any extra output, ensuring the LLM outputs only the required line.

```
    `HARD CONSTRAINTS (violating these will cause rollback):\n` +
    `${constraints}\n` +
    `- Do NOT refactor, restructure, or rewrite functions\n` +
    `- Do NOT rename variables, extract helpers, or "improve" unrelated code\n` +
    `- Do NOT add new dependencies or change public function signatures\n` +
    `- Do NOT change formatting, whitespace, or style in untouched lines\n` +
    `- ANY EXTRA OUTPUT WILL CAUSE ROLLBACK. Output ONLY the line 'MODIFIED: <filepath>' and nothing else.\n\n` +
    `MODIFIED: <filepath>`
```

## Reasoning
The torque validation likely expects the exact output format. By explicitly forbidding extra output and reminding the model to output only the modification line, we reduce the chance of verbose explanations or markdown formatting that would break the validation.

## Test Plan
Apply the change to the source file and run the F1 worker on a single repo to verify that torque exits with code 0 (or at least does not exit with code 2 due to extra output). If successful, we can run the full batch.

## Application Status
Attempted to apply the change via script on 2026-04-15; the change may require verification due to escaping issues. The source file should be checked and corrected if necessary.